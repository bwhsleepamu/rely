<% if assessment_type and Assessment::TYPES[assessment_type.to_sym]  %>
  <% Assessment::TYPES[assessment_type.to_sym][:questions].each do |question_id, question_hash|  %>
    <%= label_tag "study[results][][assessment_answers][#{question_id}]", question_hash[:text] %>
    <%= questionnaire_field(assessment, question_id, question_hash, "study[results][]") %>
  <% end %>
<% end %>
