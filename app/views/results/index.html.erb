<% @title = 'Results' %>
<div class="page-header">
  <h1>
    <%= @title %>
    <%= link_to "Create Result", new_result_path, class: 'btn btn-mini btn-primary' %>
  </h1>
</div>

<%= form_tag results_path, method: :get, remote: true, id: "results_search", class: 'form-search' do %>
  <%= hidden_field_tag :order %>
  <%= text_field_tag 'search', params[:search], class: 'search-query' %>
  <%= submit_tag 'Search', class: 'btn btn-primary' %>
<% end %>

<div id="results"><center><%= image_tag "contour/ajax-loader.gif" %></center></div>

<%= javascript_tag do %>
  $(function(){
    $.get($("#results_search").attr("action"), $("#results_search").serialize(), null, "script");
  });
<% end %>
