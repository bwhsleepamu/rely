<div class="page-header">
  <h1><%= @title %></h1>
</div>

<%= form_for @study, html: { class: 'form-horizontal' } do |f| %>
  <% if @study.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@study.errors.count, "error") %> prohibited this study from being saved:</h2>

      <ul>
      <% @study.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :original_id, nil, class: 'control-label' %>
    <div class="controls">
      <%= f.select :original_id, [['---', nil]] + Original.current.collect{|o| [o.name, o.id]} %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :study_type_id, nil, class: 'control-label' %>
    <div class="controls">
      <%= f.select :study_type_id, [['---', nil]] + StudyType.current.collect{|s| [s.name, s.id]} %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :location, nil, class: 'control-label' %>
    <div class="controls">
      <%= f.text_field :location %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :deleted, nil, class: 'control-label' %>
    <div class="controls">
      <%= f.check_box :deleted %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, class: 'btn btn-primary' %>
    <%= cancel %>
  </div>
<% end %>
