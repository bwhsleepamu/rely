<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th><%= sort_field_helper(params[:order], 'title', 'Title') %></th>
      <th><%= sort_field_helper(params[:order], 'project', 'Project') %></th>
      <th><%= sort_field_helper(params[:order], 'procedure', 'Procedure') %></th>
      <th><%= sort_field_helper(params[:order], 'assessment_type', 'Assessment Type') %></th>
      <th><%= sort_field_helper(params[:order], 'created_at', 'Created') %></th>
      <th>Actions</th>

    </tr>
  </thead>

  <tbody>
    <% @rules.each do |rule| %>
      <tr>
        <td><%= link_to rule, rule %></td>
        <td><%= link_to rule.project, rule.project %></td>
        <td><%= rule.procedure %></td>
        <td><%= rule.assessment_type %></td>
        <td><%= simple_time rule.created_at %>
        <td>
          <%= link_to 'Edit', edit_rule_path(rule), class: 'btn btn-xs' %>
          <%= link_to 'Delete', rule, method: :delete, class: 'btn btn-xs btn-danger', data: { confirm: "Are you sure you want to delete Rule #{rule.name}?" } %>
        </td>
      </tr>
    <% end %>
   </tbody>
</table>

<div class="center"><%= paginate @rules, theme: 'contour' %></div>



