<h3>Dashboard for Exercise <%= @exercise.name %></h3>
<dl class="dl-horizontal">
  <dt>Admin</dt>
  <dd><%= link_to @exercise.admin.name, @exercise.admin if @exercise.admin %></dd>

  <dt>Rule</dt>
  <dd>
    <%= link_to @exercise.rule.name, @exercise.rule if @exercise.rule %>
    <p><%= @exercise.rule.procedure %></p>
  </dd>

  <dt>Description</dt>
  <dd><%= simple_format @exercise.description %></dd>

  <dt>Assigned At</dt>
  <dd><%= @exercise.assigned_at %></dd>
</dl>

<div id="assigned_studies">
  <h3>Assigned Studies</h3>
  <table class="table table-striped table-bordered">
    <thead>
      <tr>
        <th>Group Name</th>
        <th>Study ID</th>
        <th>Study Type</th>
        <th>Location</th>
        <th>Scored?</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
    <% current_user.exercise_reliability_ids(@exercise).each do |r_id| %>
      <tr class="study">
        <td><%= r_id.group.name %></td>
        <td class="reliability_id"><%= r_id.unique_id %></td>
        <td><%= r_id.study.study_type.name %></td>
        <td><%= r_id.study.location %></td>
        <td><%= r_id.has_result? %></td>
        <td>
          <% if r_id.has_result? %>
            <%= link_to("Edit Result", edit_result_path(id: r_id.result.id)) %>
          <% else %>
            <%= link_to("Add Result", new_result_path(reliability_id: r_id.id)) %>
          <% end %>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>

</div>